<?php

use Illuminate\Database\Seeder;
use App\User;
use App\Book;

class BookUserTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
    	// $books = Book::all();
    	// $users = User::all();
     //    foreach ($books as $book) {
     //        foreach ($users as $user) {
     //            $book->followers()->save($user);
     //        }
     //    }
    }
}
